# Optional: a fine-grained GitHub PAT with NO scopes (read-only public data).
# Without this, you'll hit the unauthenticated rate limit fast (60 req/hour).
# With it, you get 5000 req/hour.
GITHUB_TOKEN=

# Optional override for OG metadata URLs in dev.
# NEXT_PUBLIC_BASE_URL=http://localhost:3000
